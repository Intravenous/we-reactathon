import React from 'react'

//Brackets as opposed to {} is an implict return and so the word return is not required after (
const Home = () => (
  <section className="Hero hero is-fullheight">
    <div className="hero-body">
      <div className="container">
        <h1 className="title is-1 has-text-centered">BarTender</h1>
        <h2 className="subtitle is-2 has-text-centered">Input your Favourite Spirit and get a list of Cocktails</h2>
      </div>
    </div>
  </section>
)

export default Home